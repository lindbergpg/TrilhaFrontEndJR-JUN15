import styled from 'styled-components'
import { colors } from '../../styles'

export const TitleSection = styled.h3`
  font-weight: bold;
  font-size: 24px;
  padding-bottom: 16px;
  color: ${colors.red};
`
